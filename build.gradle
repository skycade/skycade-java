plugins {
    id "java"
}

subprojects {

    group = 'net.skycade.java'
    version = '1.0.0-SNAPSHOT'

    project.ext.name = 'skycade-java'
    project.ext.buildVersion = project.version

    repositories {
        mavenCentral()
    }
}